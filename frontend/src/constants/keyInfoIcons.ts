import { Ionicons } from '@expo/vector-icons'

export const KEY_INFO_ICONS: Record<string, keyof typeof Ionicons.glyphMap> = {
   city: 'location-outline',
   architect: 'person-outline',
   year: 'calendar-outline',
   style: 'business-outline',
}
