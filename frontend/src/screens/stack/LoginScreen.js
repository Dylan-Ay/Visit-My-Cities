import {
   ContentContainer,
   ScreenWrapper,
   SectionTitle,
} from '../../components/ui'

export const LoginScreen = ({}) => {
   return (
      <ScreenWrapper>
         <ContentContainer style={{ flex: 1 }}>
            <SectionTitle>Connexion</SectionTitle>
         </ContentContainer>
      </ScreenWrapper>
   )
}
