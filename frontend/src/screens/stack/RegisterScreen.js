import {
   ContentContainer,
   ScreenWrapper,
   SectionTitle,
} from '../../components/ui'

export const RegisterScreen = ({}) => {
   return (
      <ScreenWrapper>
         <ContentContainer style={{ flex: 1 }}>
            <SectionTitle>Inscription</SectionTitle>
         </ContentContainer>
      </ScreenWrapper>
   )
}
